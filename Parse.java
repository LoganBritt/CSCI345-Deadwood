
public class Parse {
    // parseCard Method parses the cards and returns the values
    public static Card parseCard(Card card){
        return card;
    }

    // parseBoard method parses the board and returns the values
    public static Board parseBoard(Board board){
        return board;
    }




}
